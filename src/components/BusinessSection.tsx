const BusinessSection = () => {
  return (
    <section>
      <div className="max-w-7xl mx-auto px-3 py-2 bg-primary text-white">
        Grow Business Section
      </div>
    </section>
  );
};

export default BusinessSection;
